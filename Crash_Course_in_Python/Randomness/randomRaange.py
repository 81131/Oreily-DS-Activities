#Random range is used to generate a randomnumber between a given range

import random

num1 = random.randrange(8,15)
print(num1)
#This will genearte a number between 8(Inclusive) & 15(Exclusive). As seed is not set every time the script run, it will generate a different number